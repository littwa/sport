export enum EPhraseDifficult {
    Easy,
    Middle,
    Hard,
}

export enum EPhraseType {
    Phrase = 'phrase',
    Word = 'word',
    Collocation = 'collocation',
}

export enum EPhraseResults {
    Excellent = 'excellent',
    Mediocre = 'mediocre',
    Flop = 'flop',
}
