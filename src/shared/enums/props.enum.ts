export enum EUnit {
    kg = 'kg',
    box = 'box',
    pcs = 'pcs',
}

export enum EOrderStatus {
    New = 'new',
    Canceled = 'canceled',
    InProgress = 'in progress',
    Deliverred = 'deliverred',
    Completed = 'completed',
}
