export enum EComposeType {
    Image = 'image',
    Video = 'video',
    Audio = 'audio',
}

export enum EMediaType {
    Image = 'image',
    Video = 'video',
    Audio = 'auto',
    Raw = 'raw'
}

export type MediaTypes = 'image' | 'video' |'auto' |'raw';
